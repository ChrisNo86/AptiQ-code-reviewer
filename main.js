const codeInput =
    document.querySelector(
        "#codeInput"
    );

const analyzeBtn =
    document.querySelector(
        "#analyzeBtn"
    );

const exampleBtn =
    document.querySelector(
        "#exampleBtn"
    );

const clearBtn =
    document.querySelector(
        "#clearBtn"
    );

const scoreCard =
    document.querySelector(
        "#scoreCard"
    );

const statsCard =
    document.querySelector(
        "#statsCard"
    );

const analysisCard =
    document.querySelector(
        "#analysisCard"
    );

const scoreValue =
    document.querySelector(
        "#scoreValue"
    );

const analysisOutput =
    document.querySelector(
        "#analysisOutput"
    );